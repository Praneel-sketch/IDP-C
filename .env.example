# CityChange environment configuration.
# Copy to .env (never commit .env) or export in your shell.
# All variables are optional in v0.1 — the annual land-cover source needs no credentials.

# Override where downloaded rasters and generated outputs are stored.
# Default: ./data relative to the repository root.
#CITYCHANGE_DATA_DIR=/absolute/path/to/data
