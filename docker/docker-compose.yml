services:
  citychange:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - citychange-data:/data
    restart: unless-stopped

volumes:
  citychange-data:
